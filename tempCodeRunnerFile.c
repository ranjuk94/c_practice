#include<stdio.h>
#define max 50
void main()
{
    int l,arr[max],i,j;
    printf("Enter a limit:");
    scanf("%d",&l);
    printf("Enter the array elements:");
    for(i=0;i<l;i++)
    {
        scanf("%d",&a[i]);
    }
        for(i=l-1;i<a[0];i--)
    {
        a[j]=a[i];

    }
    printf("The reverse array is:%d "a[i]);
    }
    
